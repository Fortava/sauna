<section class="page-content" {{ section.shopify_attributes }}>
  <h1>{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</section>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
